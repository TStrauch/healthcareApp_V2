export declare class Exercise {
    id: number;
    name: string;
    description: string;
    duration: number;
    goal: string;
    image: string;
    constructor(id?: number, name?: string, description?: string, duration?: number, goal?: string, image?: string);
}
